name: Deploy to Server

on:
  push:
    branches: [ main ]
  workflow_dispatch: # Позволяет запускать вручную

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Run linting
      run: npm run lint || echo "Linting completed with warnings"
      
    - name: Build project
      run: npm run build

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_PASSWORD }}
        timeout: 600s
        command_timeout: 120s
        script: |
          cd /var/www/aldalinde || exit 1
          echo "🚀 Начинаем деплой..."
          
          # Проверяем доступность сервера
          echo "🔍 Проверяем ресурсы сервера..."
          free -h
          df -h
          
          # Останавливаем процессы осторожно
          echo "🛑 Останавливаем все процессы..."
          if pm2 list | grep -q aldalinde; then
            echo "Останавливаем PM2 процесс..."
            pm2 stop aldalinde || true
            sleep 3
            pm2 delete aldalinde || true
          else
            echo "PM2 процесс не найден"
          fi
          
          # Убиваем оставшиеся процессы
          echo "Проверяем оставшиеся процессы..."
          pkill -f "node.*next" || echo "Node процессы не найдены"
          pkill -f "npm.*start" || echo "NPM процессы не найдены"
          sleep 2
          
          # Получаем код
          echo "📥 Получаем код..."
          git stash push -m "Auto stash before deploy" || echo "Нечего сохранять в stash"
          git pull origin main || exit 1
          
          # Устанавливаем зависимости
          echo "📦 Устанавливаем зависимости..."
          if ! npm ci --silent --maxsockets 1 --prefer-offline; then
            echo "Ошибка при установке зависимостей"
            exit 1
          fi
          
          # Собираем проект
          echo "🔨 Собираем проект..."
          if ! npm run build; then
            echo "Ошибка при сборке проекта"
            exit 1
          fi
          
          # Запускаем приложение
          echo "▶️ Запускаем приложение..."
          if pm2 start npm --name "aldalinde" -- run start; then
            echo "Приложение запущено успешно"
          else
            echo "Ошибка при запуске приложения"
            exit 1
          fi
          
          sleep 5
          
          # Сохраняем конфигурацию
          echo "💾 Сохраняем конфигурацию PM2..."
          pm2 save
          
          # Проверяем статус
          echo "✅ Деплой завершен успешно!"
          pm2 status
          echo "🌐 Приложение доступно по адресу: http://62.181.44.89:3000"
